#include<iostream>
using namespace std;

int main()
{
    // int a = 0, b = 0;
    // for (i = 0; i < N; i++) {
    // a = a + rand();
    // }
    // for (j = 0; j < M; j++) {
    // b = b + rand();
    // }
    return 0;
}

// loop 1 - N times
// loop 2 - M times
// Overall Time Complexity O(N+M)